#include<iostream>
using namespace std;
int main()
{
	int a;
	char b;
	float c;
	cout<<"the size of int is:"<<sizeof(a)<<"\n";
	cout<<"the size of char is:"<<sizeof(b)<<"\n";
	cout<<"the size of float is:"<<sizeof(c)<<"\n";
}
